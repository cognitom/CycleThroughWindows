//--not for batch--// MIT License © 2012 Tsutomu Kawamura / CogniTom Academic Design// Get the Current Document Index// TODO: もっと早い実装方法を探す!function getCurrentDocumentIndex() {	var curentDocument = fw.getDocumentDOM();	for (var i = 0; i < fw.documents.length; i++)		if (fw.documents[i].javascriptString == curentDocument.javascriptString)			return i;	return -1;}var n = getCurrentDocumentIndex();n++;if (n >= fw.documents.length)	n = 0;fw.setActiveWindow(fw.documents[n]);